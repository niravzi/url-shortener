import styled from 'styled-components';
import { Flex } from '../components';

export const CenteredLayout = styled(Flex)`
  height: 100vh;
  align-items: center;
  justify-content: center;
  flex-direction: column;
`;

